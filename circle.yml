machine:
   python:
      version: 3.6.0

deployment:
   production:
       branch: master
       commands:
           - ip addr show
           - ssh -6 -v pavel@eee.home.zagrebelin.ru "cd voice_machine && git pull origin master"